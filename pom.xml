<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <groupId>com.bitvelocity</groupId>
  <artifactId>bv-core-common</artifactId>
  <version>1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Core Common</name>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub BitVelocity Packages</name>
      <url>https://maven.pkg.github.com/nitinkc/bv-core-common</url>
    </repository>
  </distributionManagement>

  <parent>
    <groupId>com.github.nitinkc</groupId>
    <artifactId>bit-velocity-parent</artifactId>
    <version>release-01.3</version>
    <relativePath/> <!-- Forces Maven to fetch from remote, not local file -->
  </parent>

  <!-- Aggregator POM should not have dependencies. If needed, move these to child modules. -->
  <modules>
    <module>bv-common-entities</module>
    <module>bv-common-events</module>
    <module>bv-common-security</module>
    <module>bv-common-auth</module>
    <module>bv-common-logging</module>
    <module>bv-common-exceptions</module>
  </modules>
</project>